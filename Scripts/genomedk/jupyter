#!/bin/bash

# Start JupyterLab with the container's built-in Jupyter configuration.
apptainer exec --fakeroot --writable-tmpfs ngs-summer-course.sif BROWSER=/usr/bin/firefox jupyter lab --port="'$UID'" --ip="'$(hostname)'" --allow-root